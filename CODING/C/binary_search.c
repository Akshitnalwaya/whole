#include<stdio.h>
int main()
{
    int count;
    int a[count],i,j,search,k;
    scanf("%d",&count);
    for ( i = 0; i < count; i++)
    {
        /* code */printf("Enter");
        scanf("%d",&a[i]);
    }
    printf("search");
    scanf("%d",&search);
    
}